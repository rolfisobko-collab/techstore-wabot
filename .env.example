PORT=8000

# Firebase Admin SDK — paste the full JSON from Firebase Console > Project Settings > Service Accounts > Generate new private key
# Must be a single line (minified JSON), e.g.: {"type":"service_account","project_id":"techstore-d12a3",...}
FIREBASE_SERVICE_ACCOUNT=
